export type Position = {
  x: number;
  y: number;
  z?: number;
};

export type Rotation = {
  angle: number;
};

export type Dimension = {
  w: number;
  h: number;
};

export type OptionalDimension = {
  w?: number;
  h?: number;
};
